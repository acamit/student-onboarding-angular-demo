import { DocumentsValidatorDirective } from './documents-validator.directive';

describe('DocumentsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DocumentsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
